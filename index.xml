<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>С Днем Святого Валентина! ❤️</title>
    <style>
        :root {
            --pink: #ff4d6d;
            --dark-pink: #c9184a;
        }

        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: url('bg.png') no-repeat center center/cover;
            overflow: hidden;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            width: 85%;
            max-width: 400px;
            z-index: 10;
        }

        h1 { color: var(--dark-pink); font-size: 1.5rem; margin-bottom: 1.5rem; }

        .btn-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            min-height: 60px;
            position: relative;
        }

        button {
            padding: 12px 25px;
            font-size: 1.1rem;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: bold;
            transition: transform 0.2s;
        }

        #yesBtn {
            background-color: var(--pink);
            color: white;
            box-shadow: 0 4px 15px rgba(255, 77, 109, 0.3);
        }

        #noBtn {
            background-color: #6c757d;
            color: white;
            position: absolute; /* Оставляем для функции убегания */
            right: 20px;
        }

        .hidden { display: none !important; }

        .result img {
            width: 100%;
            border-radius: 15px;
            margin: 15px 0;
        }

        .result p { font-size: 1.1rem; color: #333; line-height: 1.4; }
    </style>
</head>
<body>

    <div class="glass-card">
        <div id="step1">
            <h1>Ты будешь моей Валентинкой? ❤️</h1>
            <div class="btn-container">
                <button id="yesBtn">ДА!</button>
                <button id="noBtn">Нет</button>
            </div>
        </div>

        <div id="step2" class="hidden">
            <div class="result">
                <h1>Ура! Я люблю тебя! 😍</h1>
                <img src="photo.jpg" alt="Мы вместе" />
                <p>С праздником! Пусть этот день будет таким же чудесным, как и ты! ✨</p>
            </div>
        </div>
    </div>

    <audio id="music" src="music.mp3" loop="loop"></audio>

    <script>
        const yesBtn = document.getElementById('yesBtn');
        const noBtn = document.getElementById('noBtn');
        const step1 = document.getElementById('step1');
        const step2 = document.getElementById('step2');
        const audio = document.getElementById('music');

        // Функция убегания кнопки "Нет"
        const moveNo = () => {
            const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
            const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);
            noBtn.style.position = 'fixed';
            noBtn.style.left = x + 'px';
            noBtn.style.top = y + 'px';
        };

        noBtn.addEventListener('mouseover', moveNo);
        noBtn.addEventListener('touchstart', (e) => {
            e.preventDefault();
            moveNo();
        });

        // Нажатие "Да"
        yesBtn.addEventListener('click', () => {
            step1.classList.add('hidden');
            step2.classList.remove('hidden');
            audio.play().catch(() => console.log("Нужен клик для музыки"));
        });
    </script>
</body>
</html>
